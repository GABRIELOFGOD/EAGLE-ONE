import 'package:flutter/material.dart';

class ChatCardForMessageView extends StatelessWidget {
  const ChatCardForMessageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}